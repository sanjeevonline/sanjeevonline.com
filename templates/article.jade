extends layout

block prepend title
  | #{ page.title } -

block header
  include author
  a.back(href='/blog.html') << back to index
  h3= page.title
  p
    | written by:
    mixin author(page.metadata.author)

block content
  article.post
    section.content!= page.html